# Contributing Guidelines

- Use clear commit messages (e.g., `feat: add table component`)
- Keep pull requests small and focused
- Use TypeScript with strict types
- Follow atomic architecture (atoms → molecules → organisms → pages)
- Use reusable components — never duplicate UI logic
